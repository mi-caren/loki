package main

import (
	"fmt"
	"os"
)

// #include <unistd.h>

// #include "editor.h"

// int main(int argc, char *argv[]) {
func main() {
	editor := newEditor()

	// if (argc >= 2) {
	//     if (editorOpen(argv[1]) != 0) {
	//         die("Error while opening file");
	//     };
	// }

	// editorRun();

	// return 0;
}
